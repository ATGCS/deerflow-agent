# DeerFlow 前端部署检查清单

**版本**: 1.0  
**更新日期**: 2026-04-05  
**状态**: 生产就绪

---

## 📋 部署前检查

### 1. 代码检查 ✅

- [x] 所有核心代码已提交
- [x] 代码审查通过
- [x] 无 ESLint 错误
- [x] 代码注释完整
- [x] TypeScript 类型定义完整（如使用）

### 2. 功能检查 ✅

- [x] API 客户端封装完成
- [x] SSE 事件流正常
- [x] 嵌入式任务仪表板正常
- [x] 浮动任务面板正常
- [x] 对话面板功能正常
- [x] 状态持久化正常
- [x] 页面刷新恢复正常

### 3. 样式检查 ✅

- [x] CSS 样式完整
- [x] 明暗主题支持
- [x] 响应式布局正常
- [x] 动画效果流畅
- [x] 移动端适配正常

### 4. 文档检查 ✅

- [x] 用户文档完整
- [x] API 文档完整
- [x] 集成指南完整
- [x] 部署文档完整

---

## 🔧 环境准备

### Node.js 环境

```bash
# 检查 Node.js 版本（推荐 18+）
node --version

# 检查 npm 版本
npm --version
```

### 依赖安装

```bash
cd deerpanel

# 安装依赖
npm install

# 或使用 pnpm
pnpm install
```

### 环境变量配置

```bash
# 创建 .env 文件
cp .env.example .env

# 编辑 .env 文件
# API_BASE_URL=http://localhost:8000
# VITE_APP_TITLE=DeerFlow
```

---

## 🚀 开发环境部署

### 1. 启动开发服务器

```bash
cd deerpanel

# 启动 Vite 开发服务器
npm run dev

# 或
pnpm dev
```

### 2. 访问应用

打开浏览器访问：`http://localhost:5173`

### 3. 热重载

开发服务器支持热重载，修改代码后自动刷新

---

## 📦 生产环境部署

### 方案 1: Vite Build + 静态托管

#### Step 1: 构建生产版本

```bash
cd deerpanel

# 构建
npm run build

# 或
pnpm build
```

#### Step 2: 预览构建结果

```bash
# 本地预览
npm run preview

# 或
pnpm preview
```

#### Step 3: 部署到服务器

构建产物在 `dist/` 目录，可以部署到：

- **Nginx**
- **Apache**
- **Vercel**
- **Netlify**
- **Cloudflare Pages**
- **阿里云 OSS**
- **腾讯云 COS**

#### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/deerpanel/dist;
    index index.html;

    # SPA 路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理
    location /api/ {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

### 方案 2: Docker 部署

#### Dockerfile

```dockerfile
# 构建阶段
FROM node:18-alpine AS builder

WORKDIR /app

# 复制 package.json
COPY package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制源代码
COPY . .

# 构建
RUN npm run build

# 生产阶段
FROM nginx:alpine

# 复制构建产物
COPY --from=builder /app/dist /usr/share/nginx/html

# 复制 Nginx 配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

#### docker-compose.yml

```yaml
version: '3.8'

services:
  deerflow-frontend:
    build:
      context: ./deerpanel
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - deerflow-backend
    environment:
      - API_BASE_URL=http://deerflow-backend:8000

  deerflow-backend:
    image: your-registry/deerflow-backend:latest
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://user:pass@db:5432/deerflow
    depends_on:
      - db

  db:
    image: postgres:15
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
      - POSTGRES_DB=deerflow
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
```

#### 构建和运行

```bash
# 构建镜像
docker-compose build

# 启动服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

### 方案 3: Vercel 部署

#### Step 1: 安装 Vercel CLI

```bash
npm install -g vercel
```

#### Step 2: 登录 Vercel

```bash
vercel login
```

#### Step 3: 部署

```bash
cd deerpanel

# 部署到预览环境
vercel

# 部署到生产环境
vercel --prod
```

### 方案 4: Netlify 部署

#### Step 1: 安装 Netlify CLI

```bash
npm install -g netlify-cli
```

#### Step 2: 登录 Netlify

```bash
netlify login
```

#### Step 3: 部署

```bash
cd deerpanel

# 部署
netlify deploy --prod
```

---

## 🧪 部署后测试

### 功能测试清单

- [ ] 页面加载正常
- [ ] API 调用正常
- [ ] SSE 连接正常
- [ ] 浮动面板拖动正常
- [ ] 快捷键响应正常
- [ ] 状态恢复正常
- [ ] 明暗主题切换正常
- [ ] 移动端显示正常

### 性能测试

```bash
# 使用 Lighthouse 测试
# Chrome DevTools -> Lighthouse -> Run audit

# 目标分数:
# - Performance: > 90
# - Accessibility: > 90
# - Best Practices: > 90
# - SEO: > 90
```

### 兼容性测试

- [ ] Chrome (最新)
- [ ] Firefox (最新)
- [ ] Safari (最新)
- [ ] Edge (最新)
- [ ] iOS Safari
- [ ] Android Chrome

---

## 📊 监控和日志

### 前端监控

#### 1. 错误监控

```javascript
// 全局错误处理
window.addEventListener('error', (e) => {
  console.error('Global error:', e.error)
  // 发送到监控服务
})

window.addEventListener('unhandledrejection', (e) => {
  console.error('Unhandled promise rejection:', e.reason)
  // 发送到监控服务
})
```

#### 2. 性能监控

```javascript
// 使用 Performance API
window.addEventListener('load', () => {
  const perf = performance.getEntriesByType('navigation')[0]
  console.log('Page load time:', perf.loadEventEnd - perf.fetchStart)
  
  // 发送到监控服务
})
```

#### 3. 用户行为分析

集成以下服务（可选）：

- Google Analytics
- Mixpanel
- Amplitude
- 神策数据

### 日志服务

推荐服务：

- **Sentry** - 错误监控
- **LogRocket** - 会话回放
- **Datadog** - 全栈监控
- **New Relic** - 应用性能监控

---

## 🔒 安全加固

### 1. HTTPS 强制

```nginx
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name your-domain.com;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    # ... 其他配置
}
```

### 2. 安全头配置

```nginx
add_header X-Frame-Options "SAMEORIGIN" always;
add_header X-Content-Type-Options "nosniff" always;
add_header X-XSS-Protection "1; mode=block" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
add_header Content-Security-Policy "default-src 'self' http: https: ws: wss: 'unsafe-inline' 'unsafe-eval';" always;
```

### 3. API 安全

- 使用 JWT 或其他认证机制
- 实施请求限流
- 验证输入数据
- 防止 CSRF 攻击

---

## 📈 性能优化

### 1. 代码分割

```javascript
// 动态导入实现代码分割
const FloatingTaskPanel = () => import('./components/FloatingTaskPanel.js')
const TaskConversationPanel = () => import('./components/TaskConversationPanel.js')
```

### 2. 资源预加载

```html
<!-- 在 index.html 中添加 -->
<link rel="preload" href="/assets/main.js" as="script">
<link rel="preload" href="/assets/style.css" as="style">
```

### 3. 图片优化

- 使用 WebP 格式
- 实施懒加载
- 使用 CDN

### 4. CDN 加速

推荐 CDN 服务：

- Cloudflare
- 阿里云 CDN
- 腾讯云 CDN
- 七牛云

---

## 🔄 持续集成/持续部署 (CI/CD)

### GitHub Actions 示例

```yaml
# .github/workflows/deploy.yml

name: Deploy Frontend

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build
        run: npm run build
      
      - name: Run tests
        run: npm test
      
      - name: Deploy to production
        if: github.ref == 'refs/heads/main'
        run: |
          # 部署脚本
          # 例如：部署到 Vercel
          npx vercel --prod --token=${{ secrets.VERCEL_TOKEN }}
```

### GitLab CI 示例

```yaml
# .gitlab-ci.yml

stages:
  - install
  - build
  - test
  - deploy

install:
  stage: install
  script:
    - npm ci
  cache:
    paths:
      - node_modules/

build:
  stage: build
  script:
    - npm run build
  artifacts:
    paths:
      - dist/

test:
  stage: test
  script:
    - npm test

deploy:
  stage: deploy
  script:
    - ./deploy.sh
  only:
    - main
```

---

## 📝 回滚方案

### 1. 版本标记

```bash
# 为每个版本打标签
git tag -a v1.0.0 -m "Release v1.0.0"
git push origin v1.0.0
```

### 2. 快速回滚

```bash
# 如果部署失败，快速回滚到上一个版本
git revert HEAD
git push origin main

# 或者使用 CI/CD 的回滚功能
```

### 3. 数据库回滚（如有）

```bash
# 准备回滚脚本
# rollback.sql
```

---

## ✅ 部署验收清单

### 功能验收

- [ ] 所有页面正常访问
- [ ] 所有功能正常工作
- [ ] API 调用正常
- [ ] 实时更新正常
- [ ] 状态恢复正常

### 性能验收

- [ ] 首屏加载 < 2 秒
- [ ] 页面 FPS > 50
- [ ] Lighthouse 分数 > 90
- [ ] API 响应 < 500ms

### 安全验收

- [ ] HTTPS 配置正确
- [ ] 安全头配置正确
- [ ] 无已知安全漏洞
- [ ] 认证授权正常

### 兼容性验收

- [ ] 主流浏览器测试通过
- [ ] 移动端测试通过
- [ ] 响应式布局正常

---

## 📖 相关文档

- [`DeerFlow 前端实现 - 100% 完成报告.md`](./DeerFlow 前端实现 - 100% 完成报告.md)
- [`后端 API 实现指南.md`](./后端 API 实现指南.md)
- [`页面状态恢复集成指南.md`](./页面状态恢复集成指南.md)

---

**部署完成后，系统即可投入使用！** 🎉

**最后更新**: 2026-04-05
